version: "3.9"

services:
  db:
    image: postgres:16
    container_name: postgres_container1
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: jwt_db
    ports:
      - "5432:5432"
    volumes:
      - pgdata_jwt:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d jwt_db"]
      interval: 5s
      timeout: 5s
      retries: 20

volumes:
  pgdata_jwt:
